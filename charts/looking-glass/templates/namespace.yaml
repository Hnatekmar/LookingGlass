{{- if .Values.namespace.create -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "looking-glass.namespace" . }}
  labels:
    {{- include "looking-glass.labels" . | nindent 4 }}
{{- end }}
